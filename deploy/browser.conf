# Standard Chromium requires more threads than the previous Node-only limit.
# No sandbox weakening; applies to whole PayGate cgroup including child browser.
[Service]
MemoryMax=768M
TasksMax=256
LimitCORE=0
